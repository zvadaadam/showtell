{
  "name": "@showtell/render",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Render orchestrator: validate → resolve refs → (TTS → measure) → compose+capture → ffmpeg mux + watermark → mp4.",
  "license": "MIT",
  "exports": {
    ".": "./src/index.ts"
  },
  "dependencies": {
    "@showtell/capture": "workspace:*",
    "@showtell/compose": "workspace:*",
    "@showtell/core": "workspace:*",
    "@showtell/hyperframes": "workspace:*",
    "@showtell/providers": "workspace:*"
  }
}
