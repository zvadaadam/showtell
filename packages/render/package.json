{
  "name": "@agent-video/render",
  "version": "0.0.0",
  "type": "module",
  "description": "Render orchestrator: validate → resolve refs → (TTS → measure) → compose+capture → ffmpeg mux + watermark → mp4.",
  "license": "MIT",
  "exports": {
    ".": "./src/index.ts"
  },
  "dependencies": {
    "@agent-video/capture": "workspace:*",
    "@agent-video/compose": "workspace:*",
    "@agent-video/core": "workspace:*",
    "@agent-video/providers": "workspace:*"
  }
}
